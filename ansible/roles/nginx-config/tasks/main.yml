---
- name: Install nginx
  apt:
    name:
    - nginx
    state: latest
    update_cache: yes

- name: Copy nginx config files to loadbalancer
  template:
    src:
    - nginx.conf.j2
    dest: /etc/nginx/nginx.conf